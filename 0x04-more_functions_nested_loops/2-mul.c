#include "main.h"

/**
 * mul - multiplies two integers
 * @y: first number to multiply
 * @z: second number to multiply
 * Return: result of multiplication between y and z
 */
int mul(int y, int z)
{
	return (y * z);
}
